# Secret-bearing local files (PATs / bootstrap) — never commit
figma-bootstrap/            # covers figma-bootstrap/nessie.md, nessie-admin.md, etc.
bootstrap.md
**/bootstrap.md

# Environment & credentials
.env
.env.*
*.pem
*.key
*.p12

# OS / editor cruft
.DS_Store
