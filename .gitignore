# Secret-bearing local files (PATs / bootstrap) — never commit
# figma-bootstrap/ covers nessie.md, nessie-admin.md, and any other file inside it
figma-bootstrap/
bootstrap.md
**/bootstrap.md

# Environment & credentials
.env
.env.*
*.pem
*.key
*.p12

# Claude Code local settings (machine paths + permission grants — never publish)
.claude/settings.local.json

# Local-only commands & their state (reference machine-specific paths)
.claude/commands/sync-tokens.md
.claude/state/

# OS / editor cruft
.DS_Store
